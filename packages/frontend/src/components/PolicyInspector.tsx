import { Check, LockKeyhole } from "lucide-react";
const rules = [["任務綁定", "TASK-Q3-092", "上下文已鎖定"], ["預算上限", "1.00 USDC", "單次呼叫"], ["商戶", "AlphaSense MCP", "已列入允許清單"], ["資產／網路", "USDC · Base", "eip155:84532"], ["有效期限", "20 分鐘", "剩餘 18 分鐘"], ["頻率", "5／分鐘", "目前 1／5"]];
export function PolicyInspector({ defending }: { defending: boolean }) { return <article className={`panel-card overflow-hidden ${defending ? "defending-ring" : ""}`}><div className="flex items-center justify-between border-b border-line/70 px-5 py-4"><div><p className="section-kicker">Live policy gate</p><h3 className="mt-1 text-sm font-semibold text-white">六維政策約束</h3></div><span className="flex items-center gap-1.5 text-[10px] font-semibold text-emerald"><LockKeyhole size={13} /> FAIL-CLOSED</span></div><div className="divide-y divide-line/55">{rules.map(([label, value, meta], index) => <div key={label} className="group grid grid-cols-[24px_1fr_auto] items-center gap-3 px-5 py-3 transition-colors hover:bg-white/[0.018]"><span className="grid h-6 w-6 place-items-center rounded-md border border-emerald/20 bg-emerald/5 font-mono text-[9px] text-emerald">0{index + 1}</span><div><p className="text-[11px] font-medium text-slate-200">{label}</p><p className="mt-0.5 font-mono text-[9px] text-slate-600">{meta}</p></div><div className="flex items-center gap-2 text-right"><span className="font-mono text-[10px] text-slate-400">{value}</span><Check size={13} strokeWidth={2.5} className="text-emerald" /></div></div>)}</div></article>; }
