import { AlertCircle, ArrowUpRight, Ban, CheckCircle2, ReceiptText } from "lucide-react";
import type { Transaction } from "../types";

export function TransactionStream({ transactions, onInspect }: { transactions: Transaction[]; onInspect: (tx: Transaction) => void }) {
  return <article className="panel-card overflow-hidden"><div className="flex items-center justify-between border-b border-line/70 px-5 py-4"><div><p className="section-kicker">不可變稽核軌跡</p><h3 className="mt-1 text-sm font-semibold text-white">交易與結算串流</h3></div><div className="flex items-center gap-2 text-[10px] text-slate-500"><span className="live-dot bg-emerald" /> EVENTS</div></div><div className="max-h-[286px] overflow-y-auto">{transactions.map((tx) => { const link = tx.mode === "live" && tx.verified ? tx.explorerUrl : undefined; const confirmed = tx.status === "settled" && tx.verified; return <div key={tx.id} className="transaction-row"><span className={`mt-0.5 grid h-7 w-7 shrink-0 place-items-center rounded-lg ${confirmed ? "bg-emerald/8 text-emerald" : tx.status === "blocked" ? "bg-alert/10 text-alert" : "bg-amber-400/10 text-amber-300"}`}>{confirmed ? <CheckCircle2 size={15} /> : tx.status === "blocked" ? <Ban size={15} /> : <AlertCircle size={15} />}</span><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><p className="truncate text-[11px] font-semibold text-slate-200">{tx.merchant}</p><p className={`shrink-0 font-mono text-[11px] font-medium ${tx.status === "blocked" ? "text-alert" : "text-white"}`}>{tx.amount}</p></div><div className="mt-1 flex min-w-0 items-center justify-between gap-3"><p className="truncate font-mono text-[9px] text-slate-600">{tx.time} · {tx.mode === "mock" ? "MOCK 模擬" : tx.status === "unknown" ? "LIVE 結果未知" : tx.txHash ? `${tx.txHash.slice(0, 10)}…${tx.txHash.slice(-6)}` : "未建立簽章"}</p>{link ? <a className="audit-link" href={link} target="_blank" rel="noreferrer" onClick={(event) => event.stopPropagation()}>Basescan <ArrowUpRight size={10} /></a> : <span className="font-mono text-[9px] text-slate-500">{tx.status === "blocked" ? "POLICY DENY" : tx.mode === "mock" ? "無鏈上連結" : "待確認"}</span>}</div></div><button type="button" className="icon-button" onClick={() => onInspect(tx)} aria-label={`檢視 ${tx.id} 收據`}><ReceiptText size={14} /></button></div>; })}</div></article>;
}
